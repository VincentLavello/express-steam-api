This is a Node implementation to grab information of Steam games and users like achievements, profile, friend list, owned games, recently played games, etc.

To run this, follow the commands.
	
	npm install
	node index.js